<Window x:Class="lab4.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:lab4"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <DockPanel>

        <ToolBar DockPanel.Dock="Top">
            <Button Content="Create" Click="Create_Click"/>
            <Button Content="Update" Click="Update_Click"/>
            <Button Content="Delete" Click="Delete_Click"/>
        </ToolBar>

        <Grid DataContext="{Binding ElementName=list, Path=SelectedItem}">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <ListBox Grid.Row="0"
                 Grid.Column="0"
                 Grid.ColumnSpan="2"
                 Margin="0,0,0,3"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 Name="list"
                 ItemsSource="{Binding}"
                 DisplayMemberPath="Name" />

            <TextBlock Grid.Row="1" Margin="5">ISBN:</TextBlock>
            <TextBox x:Name="txtISBN"
                 Grid.Row="1"
                 Grid.Column="1"
                 Text="{Binding Path=ISBN}" />

            <TextBlock Grid.Row="2" Margin="5">Name:</TextBlock>
            <TextBox x:Name="txtName"
                 Grid.Row="2"
                 Grid.Column="1"
                 Text="{Binding Path=Name}" />

            <TextBlock Grid.Row="3" Margin="5">Authors:</TextBlock>
            <TextBox x:Name="txtAuthors"
                 Grid.Row="3"
                 Grid.Column="1"
                 Text="{Binding Path=Authors}" />

            <TextBlock Grid.Row="4" Margin="5">Publisher:</TextBlock>
            <TextBox x:Name="txtPublisher"
                 Grid.Row="4"
                 Grid.Column="1"
                 Text="{Binding Path=Publisher}" />

            <TextBlock Grid.Row="5" Margin="5">Year:</TextBlock>
            <TextBox x:Name="txtYear"
                 Grid.Row="5"
                 Grid.Column="1"
                 Text="{Binding Path=Year_of_publication}" />

        </Grid>

    </DockPanel>

</Window>
